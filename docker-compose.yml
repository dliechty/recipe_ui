services:
  recipe-ui:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000}
    container_name: recipe-ui
    ports:
      - "${RECIPE_UI_PORT:-8080}:80"
    restart: unless-stopped
